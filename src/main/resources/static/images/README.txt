This is a placeholder for the actual code image.

To complete the testing, you'll need to:

1. Create an image file named "code-question-256.jpg" 
2. Place it in: src/main/resources/static/images/
3. The image should contain this Java code:

---
public class Example {
    public static void main(String[] args) {
        int x = 5;
        int y = ++x;
        System.out.println("x = " + x + ", y = " + y);
    }
}
---

You can create this image by:
- Taking a screenshot of the code in your IDE
- Using online code-to-image generators like carbon.now.sh
- Creating the image in any image editor

The system will work without the image (it will just hide the missing image), but for full testing, add the actual image file.
